import React from "react";

const styles = {
  container: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center"
  },
  separator: {
    height: "1px",
    background: "#c1c1c1"
  },
  button: {
    background: "none",
    border: "0",
    outlineWidth: "0"
  }
};

export default function Todo() {
  return <></>;
}
